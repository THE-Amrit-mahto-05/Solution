// Role constants
const ROLES = {
  VIEWER: 'viewer',
  ANALYST: 'analyst',
  ADMIN: 'admin'
};

// Record types
const RECORD_TYPES = {
  INCOME: 'income',
  EXPENSE: 'expense'
};

// User status
const USER_STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive'
};

module.exports = {
  ROLES,
  RECORD_TYPES,
  USER_STATUS
};