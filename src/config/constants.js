const ROLES = {
  VIEWER: 'viewer',
  ANALYST: 'analyst',
  ADMIN: 'admin'
};

const RECORD_TYPES = {
  INCOME: 'income',
  EXPENSE: 'expense'
};

const USER_STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive'
};

module.exports = {
  ROLES,
  RECORD_TYPES,
  USER_STATUS
};